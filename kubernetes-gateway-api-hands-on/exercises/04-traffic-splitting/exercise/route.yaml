apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: split-route
  namespace: gateway-lab
spec:
  parentRefs:
    - name: lab-gateway
  hostnames:
    - split.gateway.local
  rules:
    - backendRefs:
        - name: stable
          port: 8080
          weight: 90
        - name: canary
          port: 8080
          weight: 10
