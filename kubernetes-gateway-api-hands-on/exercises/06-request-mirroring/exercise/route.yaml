apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: mirror-route
  namespace: gateway-lab
spec:
  parentRefs:
    - name: lab-gateway
  hostnames:
    - mirror.gateway.local
  rules:
    - backendRefs:
        - name: stable
          port: 8080
      filters:
        - type: RequestMirror
          requestMirror:
            backendRef:
              name: mirror
              port: 8080
