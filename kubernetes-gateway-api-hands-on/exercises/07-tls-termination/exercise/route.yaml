apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: secure-route
  namespace: gateway-lab
spec:
  parentRefs:
    - name: lab-gateway
      sectionName: https
  hostnames:
    - secure.gateway.local
  rules:
    - backendRefs:
        - name: stable
          port: 8080
